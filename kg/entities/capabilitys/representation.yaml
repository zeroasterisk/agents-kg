aliases: []
description: Information intended to reflect a past, current, or desired state of
  a given resource, in a format that can be readily communicated via the protocol,
  and that consists of a set of representation metadata and a potentially unbounded
  stream of representation data.
id: capability:representation
name: Representation
type: Capability
