aliases: []
description: Uses HTTP POST for client-to-server messages with optional Server-Sent
  Events for streaming capabilities. This transport enables remote server communication
  and supports standard HTTP authentication methods including bearer tokens, API keys,
  and custom headers.
id: protocol:streamable-http-transport
kind: spec
name: Streamable HTTP transport
type: Protocol
